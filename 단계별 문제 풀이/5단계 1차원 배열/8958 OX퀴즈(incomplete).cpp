#include <iostream>

using namespace std;

int main(void) {
	int T, tmp, str[80][80], score;
	cin >> T;
	for (int i = 0; i < T; i++){
		int j = 0, tmp = 0;
		while (str[i][j] != NULL){ 
			if 
			score += tmp;
		}
		cout 
	}
	return 0;
}
